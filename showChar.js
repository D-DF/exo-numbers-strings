let txt = "C'était à Mégara, faubourg de Carthage, dans les jardins d'Hamilcar.";

for(let i = 0; i < txt.length; i++) {
  console.log(`Voici le caractère : ${txt[i]} et voici l'index : ${i}`)
}

let auteur = "C'est une citation de Gustave Flaubert, du livre Salammbô";

for (let i = 1; i < auteur.length; i++) {
  console.log(`Voici le nombre de caractère que contient cette phrase : ${i}`)
}